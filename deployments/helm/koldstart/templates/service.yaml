apiVersion: v1
kind: Service
metadata:
  name: {{ include "koldstart.fullname" . }}
  labels:
    {{- include "koldstart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "koldstart.selectorLabels" . | nindent 4 }}
